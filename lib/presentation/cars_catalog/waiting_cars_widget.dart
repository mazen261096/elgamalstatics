import 'package:flutter/material.dart';

class WaitingCarsWidget extends StatelessWidget {
  const WaitingCarsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Placeholder());
  }
}
